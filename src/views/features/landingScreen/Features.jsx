import { memo } from 'react';
import s from '../../../assets/scss/landingScreen/features.module.scss';

const Features = () => {
	return <div className={s.featuresContainer}>Features</div>;
};

export default memo(Features);
