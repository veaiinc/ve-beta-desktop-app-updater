import React, { memo } from 'react';
import '../../../assets/scss/onboarding/index.scss';

const WorkspaceHandleName = () => {
	return <div className="workspace-handle-name-container"></div>;
};

export default memo(WorkspaceHandleName);
