{
  "name": "boring.notch",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "xcodebuild -project boringNotch.xcodeproj -scheme boringNotch -configuration Debug -destination 'platform=macOS' -derivedDataPath build build && open ./build/Build/Products/Debug/boringNotch.app",
    "build": "xcodebuild -project boringNotch.xcodeproj -scheme boringNotch -configuration Release -destination 'platform=macOS' -derivedDataPath build build",
    "clean": "rm -rf build"
  }
}


