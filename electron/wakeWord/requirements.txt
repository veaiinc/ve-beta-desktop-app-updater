# Core dependencies for wake word detection
numpy==1.24.3
onnxruntime==1.16.3

# Audio processing - PyAudio installation methods:
# Option 1: pip install pyaudio==0.2.11
# Option 2: pip install pipwin && pipwin install pyaudio
# Option 3: pip install pyaudio --only-binary=all
pyaudio==0.2.11
