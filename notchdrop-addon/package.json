{
	"name": "notchdrop-addon",
	"version": "1.0.0",
	"description": "Native NotchDrop addon with React UI",
	"main": "index.js",
	"scripts": {
		"install": "node-gyp rebuild",
		"build": "node-gyp rebuild",
		"build:ui": "vite build",
		"build:all": "npm run build && npm run build:ui",
		"dev:ui": "vite",
		"test:ui": "node test-ui.js",
		"clean": "rimraf build dist",
		"rebuild": "npm run clean && npm run build"
	},
	"dependencies": {
		"react": "^18.2.0",
		"react-dom": "^18.2.0"
	},
	"devDependencies": {
		"@types/react": "^18.2.0",
		"@types/react-dom": "^18.2.0",
		"@vitejs/plugin-react": "^4.0.0",
		"vite": "^4.4.0",
		"sass": "^1.64.0",
		"rimraf": "^6.0.1"
	},
	"keywords": [
		"notchdrop",
		"native",
		"addon",
		"ui"
	],
	"author": "Your Name",
	"license": "MIT"
}
